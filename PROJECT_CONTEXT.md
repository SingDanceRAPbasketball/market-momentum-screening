# 项目上下文

## 项目目标

将参考报告《全市场趋势研究 20260821.html》改造成每日收盘后自动执行的程序：从扶摇金融数据 API 获取 A 股行情，完成指标计算和质量校验，最终生成交互式静态 HTML。

## 参考报告的数据口径

- 数据范围：A 股全市场。
- 价格口径：前复权日线。
- 20 日涨幅：`最新前复权收盘价 / 20 个交易日前前复权收盘价 - 1`。
- MA20、MA60：前复权收盘价的简单移动平均。
- 20 日均额：最近 20 根日 K 成交额均值，单位为亿元。

### 趋势状态

- 强趋势：`C > MA20 > MA60`
- 修复：`C > MA20` 且 `MA20 <= MA60`
- 回调：`C <= MA20` 且 `MA20 > MA60`
- 弱趋势：`C <= MA20 <= MA60`
- `na`：均线数据不足

状态判断必须使用未取整数值，显示时再四舍五入。

### 流动性分层

- T1：20 日均额大于等于 20 亿元
- T2：大于等于 5 亿元且小于 20 亿元
- T3：大于等于 1 亿元且小于 5 亿元
- T4：小于 1 亿元

## 扶摇 API 方案

基础地址：`https://fuyao.aicubes.cn`。

- 标的列表：`GET /api/meta/tickers/list?asset_type=a-share`
- 交易日历：`GET /api/a-share/calendar/trading-days`
- 单股历史行情：`GET /api/a-share/prices/historical`
- 全市场日 K：优先使用 Market Dumps 的 Parquet 下载接口
- 复权事件：使用全市场复权事件 dump，单股接口用于抽样校验

标准 API 请求使用 `X-api-key`。服务端业务错误也可能返回 HTTP 200，因此客户端必须检查响应信封中的 `code`。

历史行情接口一次只允许一个 `thscode`，不适合作为每日全市场主链路。主链路应使用最近 10 个交易日的全市场 Parquet；单股 `adjust=forward` 只用于复权结果抽样对账。

## 数据存储方案

项目不依赖个人电脑长期保存全量数据，也不要求在线关系型数据库。

推荐线上结构：

- 对象存储保存滚动 120 个交易日行情。
- 云端任务使用临时 DuckDB 计算，任务结束后销毁中间文件。
- 保存 `latest.html` 和按日期归档的报告。
- 首次初始化可临时下载 10 年全量数据，截取最近 120 个交易日后删除全量文件。

推荐部署选项：

1. 国内稳定性优先：腾讯云 COS + SCF 定时云函数。
2. 低成本原型：GitHub Actions + Cloudflare R2。

## 每日任务

建议在交易日 15:37、16:07、16:37 多次触发，任务自身保持幂等：

1. 判断今日是否交易日。
2. 如果目标日期已经成功发布则退出。
3. 获取最近 10 个交易日行情。
4. 校验最大行情日期是否为目标日期；未就绪则等待下一次补偿任务。
5. 与滚动数据按 `(thscode, date_ms)` 合并去重，只保留最近 120 个交易日。
6. 计算指标、市场宽度、直方图、趋势矩阵和分层统计。
7. 先写临时 HTML，校验成功后原子替换 `latest.html`。
8. 上传滚动 Parquet、报告和运行清单。

## HTML 功能

- 市场 KPI
- 动量与流动性散点图
- 20 日涨幅直方图
- 流动性与趋势状态矩阵
- 市场宽度时间序列
- 流动性分层统计
- 涨幅、流动性层级、趋势状态、成交额联动筛选
- 当前筛选下 20 日涨幅前 15 名

生成物应为单文件 HTML，内嵌 CSS、ECharts 和报表 JSON，可离线打开。

## 已发现的参考报告问题

- 趋势矩阵的中位涨幅底层是小数收益率，原页面展示时疑似遗漏乘以 100。
- 保存页面包含交互后的 DOM 快照，控件属性和屏幕文本可能不一致。
- 部分预计算样本数之间存在差异，开发时必须明确各指标分母。
- 使用已经取整的价格和均线重算趋势状态会产生边界误差。

## 发布前校验

- 最新交易日等于目标交易日。
- `(thscode, date_ms)` 唯一。
- OHLC 关系合法，成交额非负。
- 当日股票覆盖数量未显著低于近期水平。
- 统计分母明确，矩阵计数与有效样本一致。
- HTML 不包含 `NaN` 或 `undefined`。
- API Key 和下载签名链接不得出现在日志、HTML或Git仓库。

## 下一步

1. 使用实际 API Key 验证 Market Dumps 权限和返回结构。
2. 创建 Python 项目骨架及 DuckDB 数据模型。
3. 实现数据下载、复权、指标计算和验证。
4. 将参考 HTML 重构为可重复渲染的 Jinja2 模板。
5. 配置云端定时任务和对象存储发布。

